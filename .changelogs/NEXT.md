# Unreleased Changes

## Changelog notes
- Done work logged to `NEXT.md` is now grouped under feature-named headings instead of generic Added/Changed/Fixed/Removed buckets, and entries are written for someone reading release notes rather than reading the diff — no file paths, module names, or test counts (purely internal code-organization work is the one exception, grouped under an `## Internal` heading).
